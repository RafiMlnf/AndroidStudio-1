<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_app"
        android:label="Fetch"
        android:roundIcon="@drawable/icon_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloApps"
        tools:targetApi="31" >

        <activity android:name=".Menu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ActivityHalo"/>
        <activity android:name=".chat.ActivityPesan1"/>
        <activity android:name=".ActivityIceCold"/>
        <activity android:name=".alarm.ActivityAlarm"/>
        <activity android:name=".ActivityCount" />
        <activity android:name=".chat.ActivityPesan2" />
        <activity android:name=".fragment.ViewPagerActivity" />
        <activity android:name=".Sinopsis.Sinopsis1" />
        <activity android:name=".Sinopsis.Sinopsis2" />

    </application>
</manifest>